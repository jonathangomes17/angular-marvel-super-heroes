import { ISerialize } from 'src/app/interfaces/serialize.interface';

export class CharacterComicModel implements ISerialize {

  constructor() {
  }

  serialize(): object {
    return {
    }
  }
}
